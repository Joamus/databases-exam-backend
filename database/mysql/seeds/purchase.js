module.exports.seed = function(purchase) {
  /*  purchase.bulkCreate([
        {},
        {}
    ],
    {
        updateOnDuplicates: ['address', 'basket_id']
    }) */

    console.log('[MySQL] Purchase seeded ... (NOT DONE)')
}